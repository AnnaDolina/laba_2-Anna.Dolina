#include <iostream>
#include <cmath>
using namespace std;

int main()
{
   double x ;
     double y ;
    double a = sqrt(pow(x, 3) + (y, 3) / cos(x));
    double d = a / pow(x, y);
    return 0;
}
